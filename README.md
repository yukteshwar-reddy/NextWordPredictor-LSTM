# NextWordPredictor-LSTM
Using LSTM we have developed a model for predicting the next word .Model is trained on the Shakespeare hamlet Text Dataset.The model accuracy is little low about 40(50 epochs).If i trained the model with greater number of epochs the accuracy may become better 
